<?xml version="1.0" encoding="utf-8"?>
<!-- TV盒子主界面 - 5%安全间距 + 过扫描保护 -->
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res/auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FF000000">

    <!-- 顶部标题栏 - TV盒子安全间距 -->
    <TextView
        android:id="@+id/tv_header"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="梦灯盒 - TV视频播放器"
        android:textSize="28sp"  <!-- TV盒子：超大字体，3米外观看 -->
        android:textColor="#FFFFFFFF"
        android:textStyle="bold"
        android:gravity="center"
        android:padding="24dp"  <!-- TV盒子：增加内边距 -->
        android:layout_marginTop="32dp"  <!-- 5%安全间距：防止顶部过扫描 -->
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:fontFamily="sans-serif-medium"
        android:letterSpacing="0.05"  <!-- 增加字间距：TV优化 -->
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 视频列表容器 - TV盒子优化 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_menu"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="24dp"  <!-- 底部安全间距 -->
        android:layout_marginStart="24dp"   <!-- 左侧安全间距 -->
        android:layout_marginEnd="24dp"     <!-- 右侧安全间距 -->
        android:clipToPadding="false"
        android:padding="8dp"
        app:layout_constraintTop_toBottomOf="@id/tv_header"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 底部状态栏 - TV盒子信息展示 -->
    <LinearLayout
        android:id="@+id/ll_status"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:layout_marginBottom="16dp"  <!-- 底部安全间距 -->
        android:background="#33FFFFFF"
        android:gravity="center_vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tv_status"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="准备就绪"
            android:textSize="18sp"  <!-- TV盒子：大字体 -->
            android:textColor="#FFFFFFFF"
            android:fontFamily="sans-serif-medium"/>

        <TextView
            android:id="@+id/tv_memory"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="内存: --"
            android:textSize="16sp"
            android:textColor="#FFAAAAAA"
            android:fontFamily="sans-serif-light"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>